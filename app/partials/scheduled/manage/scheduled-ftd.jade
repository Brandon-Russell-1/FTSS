include ../../__mixins
include ../__scheduled-mixins

+addButton('Create Class')

.wrapped.addNew

    +userInstructions('scheduled-ftd', true)

    // div(ui-calendar='uiConfig.calendar', calendar='schedule', ng-model='events')

    table#resourceView.table.table-bordered.table-hover

        thead
            tr: th.month(ng-repeat='month in resourceMonths', colspan='{{month.colspan}}') {{month.month}}

            tr.days(ng-bind-html='resourceDays')

        tbody

            tr.event(ng-repeat-start='event in events', ng-bind-html='event.html', ng-dblclick='event.edit()')

//
    +grouping()

        +classHeader(false)

            .col-lg-8.pull-left

                +classData(false)

            .col-lg-2.pull-right.text-right

                +seatStats()

            +notes(true)

            .clearfix

            +classActions(true)