include ../images
p.

    The first diagram below is a simple version of the SharePoint database schema.  The second diagram depicts
    the SharePoint model specified in JS.  Finally, the third diagram the scope prototypical inheritance from
    the mainController.  FTSS uses a hybrid storage approach, partly standard SP (SQL) and partly JSON documents
    within the records.  Once the data is received, it is parsed and manipulated by our Angular app.

.text-center
    +img-erd()
    +img-sp-model()
    +img-scope-tree()